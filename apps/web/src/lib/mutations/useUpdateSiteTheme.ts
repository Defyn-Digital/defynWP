// Stub — full implementation arrives in Task 25.
export function useUpdateSiteTheme(_siteId: number, _slug: string) {
  return { update: () => {}, isPending: false, isPolling: false, error: null };
}
