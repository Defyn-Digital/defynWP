// P5.1 — Client maintenance report branding tokens. Kept tiny + standalone so
// the print stylesheet and the report header band share a single source of truth.
export const REPORT_ACCENT = '#26215C';
